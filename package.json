{
  "name": "ip-city-rest-map-revive",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.10",
    "vitest": "~3.2.0",
    "wrangler": "^4.95.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
